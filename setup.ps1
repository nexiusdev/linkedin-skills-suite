# LinkedIn Skills Suite — First-time setup
# Creates client config and log files that are gitignored.
# Safe to re-run: skips files that already exist.

$ErrorActionPreference = "Stop"

Write-Host ""
Write-Host "LinkedIn Skills Suite - Setup" -ForegroundColor Cyan
Write-Host "=============================" -ForegroundColor Cyan
Write-Host ""

# --- Directories ---
$dirs = @(
    "references"
    "shared/logs"
    "linkedin-connect-timer/logs"
    "linkedin-daily-planner/shared/logs"
    "linkedin-daily-planner/logs"
    "linkedin-icp-warmer/logs"
    "linkedin-icp-finder/references"
    "web-icp-scanner"
)

foreach ($d in $dirs) {
    if (-not (Test-Path $d)) {
        New-Item -ItemType Directory -Path $d -Force | Out-Null
        Write-Host "  Created directory: $d" -ForegroundColor Green
    }
}

# --- Helper: write placeholder if file doesn't exist ---
function New-Placeholder {
    param(
        [string]$Path,
        [string]$Content
    )
    if (-not (Test-Path $Path)) {
        Set-Content -Path $Path -Value $Content -Encoding UTF8
        Write-Host "  Created: $Path" -ForegroundColor Green
    } else {
        Write-Host "  Exists:  $Path" -ForegroundColor DarkGray
    }
}

# --- Client config (generated by linkedin-onboarding) ---
$configPlaceholder = "# Not configured`n`n> Run ``linkedin-onboarding`` to generate this file."

New-Placeholder "references/icp-profile.md"            $configPlaceholder
New-Placeholder "references/contact-classification.md"  $configPlaceholder
New-Placeholder "references/connect-request.md"         $configPlaceholder
New-Placeholder "references/saved-asset.md"             $configPlaceholder
New-Placeholder "references/linkedin-strategy.md"       $configPlaceholder
New-Placeholder "references/company-pages-config.md"    $configPlaceholder

# --- Account config ---
New-Placeholder "shared/linkedin-account-config.md"                       $configPlaceholder
New-Placeholder "linkedin-daily-planner/shared/linkedin-account-config.md" $configPlaceholder

# --- Activity logs ---
$logPlaceholder = "# Activity Log`n`nNo activity recorded yet."

New-Placeholder "shared/logs/linkedin-activity.md"            $logPlaceholder
New-Placeholder "shared/logs/icp-prospects.md"                $logPlaceholder
New-Placeholder "shared/logs/linkedin-blacklist.md"           $logPlaceholder
New-Placeholder "shared/logs/inbound-screening-history.md"    $logPlaceholder
New-Placeholder "shared/logs/company-pages-activity.md"       $logPlaceholder
New-Placeholder "shared/logs/x-activity.md"                   $logPlaceholder
New-Placeholder "shared/logs/whatsapp-activity.md"            $logPlaceholder
New-Placeholder "shared/logs/instagram-activity.md"           $logPlaceholder
New-Placeholder "shared/logs/thought-leaders-agentic-ai.md"   $logPlaceholder
New-Placeholder "shared/logs/algorithm-changes.md"            $logPlaceholder
New-Placeholder "shared/logs/video-activity.md"               $logPlaceholder
New-Placeholder "linkedin-connect-timer/logs/activity-log.md" $logPlaceholder
New-Placeholder "linkedin-daily-planner/shared/logs/linkedin-activity.md" $logPlaceholder
New-Placeholder "linkedin-daily-planner/shared/logs/icp-prospects.md"     $logPlaceholder
New-Placeholder "linkedin-icp-warmer/logs/warmup-runs.md"     $logPlaceholder
New-Placeholder "linkedin-icp-finder/references/icp-profile.md" $configPlaceholder

# --- Web ICP scanner ---
New-Placeholder "web-icp-scanner/web-discovered-prospects.md"             $logPlaceholder
New-Placeholder "web-icp-scanner/icp-learning-log.md"                     $logPlaceholder
New-Placeholder "web-icp-scanner/prospect-linkedin-search-results.json"   "{}"

Write-Host ""
Write-Host "Setup complete!" -ForegroundColor Green
Write-Host ""
Write-Host "Next steps:" -ForegroundColor Yellow
Write-Host "  1. Open Codex in this directory"
Write-Host "  2. Run: linkedin-onboarding"
Write-Host "  3. Run: start linkedin"
Write-Host ""
